"""Curriculum helpers for Nezha spring jumping.

The reference task contains an unfinished distance curriculum, but keeps
``commands.curriculum`` disabled. The manager reproduction preserves that
active training behavior.
"""


def jump_distance_levels(env, env_ids):
    return None
